name: mockito_ext
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  test: ^1.16.0
  mockito: ^5.0.9
  build_runner: ^2.0.4

dev_dependencies:
  pedantic: ^1.10.0
